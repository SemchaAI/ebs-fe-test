const BASE_API_URL = 'https://fakestoreapi.com';

export enum ApiRoutes {
  PRODUCTS = `${BASE_API_URL}/products`,
  CATEGORIES = `${BASE_API_URL}/products/categories`,
  CATEGORY_PRODUCTS = `${BASE_API_URL}/products/category`,
}
